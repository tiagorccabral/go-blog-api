version: "3"
services:
  app:
    container_name: go-api-setup
    build: .
    ports:
      - 8080:8080
    restart: on-failure
    volumes:
      - api:/usr/src/app/

volumes:
  api:
  database_mysql:
